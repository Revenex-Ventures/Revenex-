{
  "functions": [
    {
      "codebase": "default",
      "source": "artifacts/api-server",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firestore-debug.log",
        "pubsub-debug.log"
      ],
      "predeploy": [
        "pnpm --filter @workspace/api-server build"
      ]
    }
  ]
}
