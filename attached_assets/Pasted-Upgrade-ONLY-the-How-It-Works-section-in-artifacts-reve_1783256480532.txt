Upgrade ONLY the "How It Works" section in artifacts/revenex/src/
Do not touch any other section. Build must pass after changes.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
HOW IT WORKS — FULL LUXURY REDESIGN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Find the HowItWorks component file and completely replace its JSX and styles.

SECTION WRAPPER:
  bg: transparent (inherits beige #F5F0E8)
  py-32 px-6
  max-w-4xl mx-auto

HEADING AREA (centered, animated):
  Label: "HOW IT WORKS"
    bg #1A1410, text white, text-xs uppercase tracking-[0.25em]
    px-5 py-2 rounded-full font-medium inline-block mb-8
    framer-motion: scale 0.7→1, opacity 0→1 on scroll, once:true

  Main heading:
    Line 1: "From signup to live —"
    Line 2: "in 4 simple" + " steps." (steps in color #8B4513 italic)
    style: text-5xl md:text-6xl font-black text-[#1A1410] leading-tight text-center
    framer-motion: y:40→0 opacity:0→1 duration:0.6 once:true

  Subtext (centered, max-w-xl mx-auto):
    "No long contracts. No complicated setup. No IT team needed.
    We handle everything — from configuration to training to go-live."
    text-[#6B5D52] text-base leading-relaxed mt-4
    framer-motion: y:20→0 opacity:0→1 delay:0.2 once:true

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TIMELINE LAYOUT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Use a LEFT-SIDE VERTICAL TIMELINE layout (not centered):
  Timeline container: relative, max-w-2xl mx-auto, mt-20

VERTICAL LINE:
  Position: absolute, left: 28px, top: 0, bottom: 0, width: 2px
  Background: linear-gradient(180deg, #1A1410 0%, #8B4513 50%, #E8E0D4 100%)
  This line is the "track"

  ANIMATED PROGRESS LINE on top of it:
    Same position, same width
    Use framer-motion useScroll + useTransform to make the line
    grow from height 0% to 100% as user scrolls through the section
    background: linear-gradient(180deg, #8B4513, #C4722A)
    This creates a "filling" effect as you scroll down

EACH STEP ITEM:
  Layout: flex row, gap-8, relative, pb-16 (except last item pb-0)
  
  LEFT SIDE — Icon + connector dot:
    Outer container: relative, flex-shrink-0, w-14 h-14
    
    Icon box: w-14 h-14, rounded-2xl, flex items-center justify-center
      Default bg: #FFFFFF, border: 2px solid #E8E0D4
      shadow: 0 4px 16px rgba(139,69,19,0.10)
      Icon: lucide-react, size 22, color #8B4513
      
    Small dot on the timeline line:
      Position: absolute, right: -8px, top: 50%, transform: translateY(-50%)
      w-4 h-4, rounded-full, bg #8B4513
      border: 3px solid #F5F0E8 (creates ring effect against beige bg)
      box-shadow: 0 0 0 3px #8B4513 (outer ring)
    
    WHEN THIS STEP IS IN VIEW (framer-motion whileInView):
      Icon box: bg → linear-gradient(135deg, #1A1410, #2D2018)
      Icon color → white
      scale: 1→1.08→1 (brief pulse, once)
      shadow: 0 8px 32px rgba(26,20,16,0.25)
      transition: spring stiffness 200

  RIGHT SIDE — Content:
    Step label: "STEP 01" / "STEP 02" etc.
      bg #F0E8DC, text #8B4513, text-xs font-bold uppercase
      tracking-[0.15em] px-3 py-1 rounded-full inline-block mb-3
    
    Step title: text-2xl font-bold text-[#1A1410] mb-2
    
    Step descriptions (2 lines):
      Line 1: font-medium text-[#3D3128]